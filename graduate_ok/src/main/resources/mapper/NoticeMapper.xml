<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org///DTO Mapper 3.0//EN" "http://mybatis.org//dtd//mybatis-3-mapper.dtd">

<mapper namespace="com.graduate_ok.graduate_ok.mapper.NoticeMapper">

    <!-- 공지사항 목록 조회 -->
    <select id="selectNoticeList" resultType="NoticeListDto">
        SELECT noti_key, noti_category, noti_title, noti_content, noti_wt_time
        FROM notice
        WHERE del_flag = 0
        LIMIT #{data_index} , 10
    </select>

    <!-- 공지사항 검색 -->
    <select id="selectNoticeByKeyword" parameterType="String" resultType="NoticeListDto">
        SELECT noti_key, noti_title, noti_category, noti_wt_time, noti_content
        FROM notice
        WHERE del_flag = 0 and noti_title = #{keyword} or noti_content = #{keyword}
    </select>

</mapper>